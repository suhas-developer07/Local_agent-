/// <reference types="node" />
export declare type CommandResult = {
    stdout: string;
};
export declare const spawn: (cmd: string, args: string[], data?: Buffer | undefined) => Promise<CommandResult>;
